# Tasks — Executable derivations

> Done when all criteria are 🟢 and the suite is ≥ **436 PASS / 0 FAIL**.

## T1 — Contract in 🔴 RED
Every criterion asserted against fixtures, one sandbox each.

## T2 — `check_90`: parse, execute, compare
Distinguish mismatch from broken command from non-integer. Name the retro, field, claim and output.

## T3 — Template and skill
`specs/_template/retro.md` carries the form, the boundary and what a later red means. The retro
skill's step 1 says derive with a command.

## T4 — Migrate the nine closed retros (`D3`, before T5)
Fix or explain whatever the check flags. 013 is already known to disagree.

## T5 — Close the RED
Every rule proved failable on its own fixture. Suite green, `nvc.sh` clean, hermetic.
