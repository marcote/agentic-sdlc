---
extends: base
---

# Ground rules — project layer

Additional ground rules for **this repository**, on top of the six in `base/ground-rules.md`.
Additive only: the base six are always in the effective set, and the engine rejects a layer that
omits any of them. To decline a rule, use an `n/a` block in the charter — not removal here.

_(none yet — the base six are the whole floor for this project)_
